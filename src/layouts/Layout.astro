---
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
}

const { title, description = 'Testen Sie die Lesbarkeit Ihrer Texte mit der Flesch-Formel' } = Astro.props;
---

<!doctype html>
<html lang="de" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} | Leichtlesbar.ch</title>
    <!-- Dark mode script (runs before page renders to prevent flash) -->
    <script is:inline>
      const theme = localStorage.getItem('theme');
      if (theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      }
    </script>
  </head>
  <body class="min-h-screen bg-gray-50 text-gray-900 antialiased transition-colors dark:bg-gray-900 dark:text-gray-100">
    <header class="bg-white shadow-sm dark:bg-gray-800 dark:shadow-gray-900/50">
      <nav class="mx-auto max-w-4xl px-4 py-4">
        <div class="flex items-center justify-between">
          <a href="/" class="flex items-center gap-2 text-xl font-bold text-blue-600 dark:text-blue-400">
            <img src="/favicon.svg" alt="" class="h-8 w-8" />
            Leichtlesbar
          </a>
          <div class="flex items-center gap-4">
            <ul class="hidden gap-6 md:flex">
              <li><a href="/" class="hover:text-blue-600 dark:hover:text-blue-400">Testen</a></li>
              <li><a href="#formel" class="hover:text-blue-600 dark:hover:text-blue-400">Flesch-Formel</a></li>
              <li><a href="#vorteile" class="hover:text-blue-600 dark:hover:text-blue-400">Vorteile</a></li>
              <li><a href="#nachteile" class="hover:text-blue-600 dark:hover:text-blue-400">Nachteile</a></li>
            </ul>
            <!-- Dark mode toggle -->
            <button
              id="theme-toggle"
              type="button"
              class="rounded-lg p-2 hover:bg-gray-100 dark:hover:bg-gray-700"
              aria-label="Farbschema wechseln"
            >
              <svg id="sun-icon" class="hidden h-5 w-5 dark:block" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd" />
              </svg>
              <svg id="moon-icon" class="block h-5 w-5 dark:hidden" fill="currentColor" viewBox="0 0 20 20">
                <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z" />
              </svg>
            </button>
            <button id="menu-toggle" class="rounded-lg p-2 hover:bg-gray-100 md:hidden dark:hover:bg-gray-700" aria-label="Menü öffnen">
              <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
        <ul id="mobile-menu" class="mt-4 hidden flex-col gap-2 md:hidden">
          <li><a href="/" class="block py-2 hover:text-blue-600 dark:hover:text-blue-400">Testen</a></li>
          <li><a href="#formel" class="block py-2 hover:text-blue-600 dark:hover:text-blue-400">Flesch-Formel</a></li>
          <li><a href="#vorteile" class="block py-2 hover:text-blue-600 dark:hover:text-blue-400">Vorteile</a></li>
          <li><a href="#nachteile" class="block py-2 hover:text-blue-600 dark:hover:text-blue-400">Nachteile</a></li>
        </ul>
      </nav>
    </header>

    <main class="mx-auto max-w-4xl px-4 py-8">
      <slot />
    </main>

    <footer class="mt-auto border-t border-gray-200 bg-white py-6 dark:border-gray-700 dark:bg-gray-800">
      <div class="mx-auto max-w-4xl px-4 text-center text-sm text-gray-600 dark:text-gray-400">
        <p>&copy; {new Date().getFullYear()} Leichtlesbar.ch</p>
      </div>
    </footer>

    <script>
      // Mobile menu toggle
      const toggle = document.getElementById('menu-toggle');
      const menu = document.getElementById('mobile-menu');
      toggle?.addEventListener('click', () => {
        menu?.classList.toggle('hidden');
        menu?.classList.toggle('flex');
      });

      // Theme toggle
      const themeToggle = document.getElementById('theme-toggle');
      themeToggle?.addEventListener('click', () => {
        const isDark = document.documentElement.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
      });
    </script>
  </body>
</html>
